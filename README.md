Le frontend a été conçu avec angular 17.

Prérequis : NodeJS, npm.

Pour installer le cli d'angular, faire : `npm install -g @angular/cli@17`
Pour charger les dépendences de notre projets faire `npm i`
Puis lancer le projet avec `ng serve`
